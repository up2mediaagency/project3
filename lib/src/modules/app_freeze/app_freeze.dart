import 'package:flutter/material.dart';

class AppFreezed extends StatelessWidget {
  const AppFreezed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("The Application is under maintenance."),
      ),
    );
  }
}
